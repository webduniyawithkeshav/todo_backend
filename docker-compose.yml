version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - '5432:5432'

  app:
    build: .
    volumes:
      - ./:/app:delegated
    working_dir: /app
    environment:
      - PYTHONUNBUFFERED=1
      - DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/postgres
      - API_URL=${API_URL}
      - API_KEY=${API_KEY}
      - CSV_PATH=/data/input.csv
    depends_on:
      - db
    ports:
      - '8000:8000'
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000

  worker:
    build: .
    volumes:
      - ./:/app:delegated
    working_dir: /app
    environment:
      - PYTHONUNBUFFERED=1
      - DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/postgres
      - API_URL=${API_URL}
      - API_KEY=${API_KEY}
      - CSV_PATH=/data/input.csv
      - THIRD_CSV_PATH=/data/third_input.csv
    depends_on:
      - db
    command: python -u -m app.etl_runner --interval 60

volumes:
  pgdata:
